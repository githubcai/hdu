#include <stdio.h>

int main(){
    int total=3600*12, temp, degrees;
    double sd=360.0/60, md=360.0/3600, hd=30.0/3600;
    while(1){
        scanf("%d", &degrees);
        if(degrees==-1){
            break;
        }
        for(temp=0;temp<total;temp++){
            
        }
    }
    return 0;
}
